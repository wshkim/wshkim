<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.in_con.www.wshkim">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".calc.CalcActivity" />
        <activity android:name=".member.JoinActivity" />
        <activity android:name=".member.CountActivity" />
        <activity android:name=".member.DeleteActivity" />
        <activity android:name=".member.DetailActivity" />
        <activity android:name=".member.ListActivity" />
        <activity android:name=".member.LoginActivity" />
        <activity android:name=".member.UpdateActivity"></activity>
    </application>

</manifest>